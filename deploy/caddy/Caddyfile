# Referencia para tu Caddy existente (fuera de este compose).
# Copia este bloque a tu Caddyfile real y ajusta el puerto si cambias SITE_PORT.

jorgeveliz.dev, www.jorgeveliz.dev {
	reverse_proxy localhost:8080
}
